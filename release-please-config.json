{
  "$schema": "https://githubusercontent.com",
  "packages": {
    ".": {
      "package-name": "@ordius/adonisjs-socketio",
      "release-type": "node",
      "pull-request-title-pattern": "chore(release): v${version}",
      "tag-format": "v${version}"
    }
  }
}
